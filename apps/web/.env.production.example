# Production build when UI is served by the same Worker as the API (hub.d1g.uk).
# Leave empty so the browser uses same-origin /v1/...
VITE_API_BASE=

# Only if API is on a different host than the UI:
# VITE_API_BASE=https://api.d1g.uk
